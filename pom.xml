<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.automation</groupId>
    <artifactId>api-automation</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>API Automation Framework</name>
    <description>REST Assured + TestNG + Allure API test automation</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>17</maven.compiler.release>
        <rest-assured.version>5.5.0</rest-assured.version>
        <testng.version>7.10.2</testng.version>
        <!-- Java adapters (Maven Central). CLI zip uses allure.report.version (GitHub release). -->
        <allure.version>2.29.1</allure.version>
        <allure.report.version>2.29.0</allure.report.version>
        <jackson.version>2.18.2</jackson.version>
        <slf4j.version>2.0.16</slf4j.version>
        <logback.version>1.5.15</logback.version>
        <playwright.version>1.49.0</playwright.version>
        <aspectj.version>1.9.22.1</aspectj.version>
        <surefire.version>3.5.2</surefire.version>
        <allure-maven.version>2.14.0</allure-maven.version>
        <extentreports.version>5.1.2</extentreports.version>
        <poi.version>5.3.0</poi.version>
        <!-- Surefire forked-JVM memory settings.
             TieredStopAtLevel=1 keeps only the lightweight C1 JIT active and
             prevents the C2 compiler from allocating large native Chunk arenas —
             the root cause of "malloc failed … for Chunk::new" crashes on
             memory-constrained Windows machines.
             SerialGC avoids the large upfront virtual-address reservation that
             G1GC requires (prevents DOS-1455 paging-file errors).
             768m heap: AspectJ weaver + TestNG + RestAssured + Allure + Jackson
             all resident simultaneously need more than 256 m; 768 m is a safe
             ceiling without triggering OS paging-file exhaustion on Windows. -->
        <argLine>-Xmx768m -XX:+UseSerialGC -XX:TieredStopAtLevel=1 -XX:ReservedCodeCacheSize=128m</argLine>
        <allure.report.skip>false</allure.report.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <version>${rest-assured.version}</version>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>json-schema-validator</artifactId>
            <version>${rest-assured.version}</version>
        </dependency>

        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>${testng.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-testng</artifactId>
            <version>${allure.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-rest-assured</artifactId>
            <version>${allure.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <dependency>
            <groupId>com.aventstack</groupId>
            <artifactId>extentreports</artifactId>
            <version>${extentreports.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>${poi.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>
        <!-- Optional browser re-login when refresh token / device session is revoked (-Pbrowser-auth) -->
        <dependency>
            <groupId>com.microsoft.playwright</groupId>
            <artifactId>playwright</artifactId>
            <version>${playwright.version}</version>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.version}</version>
                <configuration>
                    <suiteXmlFiles>
                        <suiteXmlFile>src/test/resources/testng.xml</suiteXmlFile>
                    </suiteXmlFiles>
                    <!-- Use @{argLine} only if jacoco-maven-plugin prepare-agent is bound; otherwise the JVM sees a literal @{argLine} and fails to start. -->
                    <argLine>-javaagent:"${settings.localRepository}/org/aspectj/aspectjweaver/${aspectj.version}/aspectjweaver-${aspectj.version}.jar" ${argLine}</argLine>
                    <systemPropertyVariables>
                        <env>${env}</env>
                        <allure.results.directory>${project.build.directory}/allure-results</allure.results.directory>
                    </systemPropertyVariables>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.aspectj</groupId>
                        <artifactId>aspectjweaver</artifactId>
                        <version>${aspectj.version}</version>
                    </dependency>
                </dependencies>
            </plugin>

            <!--
                Allure Trend history:
                  - Before tests: copy allure-history/ (project root) → target/allure-results/history/
                    so Allure has prior run data and can draw the Trend graph.
                  - After allure:report (same 'test' phase, runs last): copy the freshly-generated
                    history/ back to allure-history/ so it survives the next `mvn clean`.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>allure-history-restore</id>
                        <phase>generate-test-resources</phase>
                        <goals><goal>run</goal></goals>
                        <configuration>
                            <target>
                                <mkdir dir="${project.build.directory}/allure-results/history"/>
                                <copy todir="${project.build.directory}/allure-results/history"
                                      failonerror="false" quiet="true"
                                      overwrite="true">
                                    <fileset dir="${project.basedir}/allure-history"
                                             erroronmissingdir="false"/>
                                </copy>
                            </target>
                        </configuration>
                    </execution>
                    <execution>
                        <id>allure-history-save</id>
                        <phase>test</phase>
                        <goals><goal>run</goal></goals>
                        <configuration>
                            <target>
                                <mkdir dir="${project.basedir}/allure-history"/>
                                <copy todir="${project.basedir}/allure-history"
                                      failonerror="false" quiet="true"
                                      overwrite="true">
                                    <fileset dir="${project.reporting.outputDirectory}/allure-maven/history"
                                             erroronmissingdir="false"/>
                                </copy>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>io.qameta.allure</groupId>
                <artifactId>allure-maven</artifactId>
                <version>${allure-maven.version}</version>
                <configuration>
                    <!-- 2.29.1 CLI zip is not published; use latest 2.29.x release bundle from GitHub. -->
                    <reportVersion>${allure.report.version}</reportVersion>
                    <allureDownloadUrl>https://github.com/allure-framework/allure2/releases/download/${allure.report.version}/allure-${allure.report.version}.zip</allureDownloadUrl>
                    <resultsDirectory>${project.build.directory}/allure-results</resultsDirectory>
                    <reportDirectory>${project.reporting.outputDirectory}/allure-maven</reportDirectory>
                    <!-- Skip HTML generation (e.g. CI): mvn test -Dallure.report.skip=true -->
                    <skip>${allure.report.skip}</skip>
                </configuration>
                <executions>
                    <!-- mvn test only writes JSON under target/allure-results; this runs report after Surefire in the same phase. -->
                    <execution>
                        <id>allure-report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <mainClass>com.automation.api.auth.VrgoTokenManager</mainClass>
                    <!-- environments/test.properties lives under src/test/resources -->
                    <classpathScope>test</classpathScope>
                </configuration>
                <executions>
                    <execution>
                        <id>browser-recovery-verify</id>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                            <mainClass>com.automation.api.auth.VrgoBrowserRecoveryVerifier</mainClass>
                            <classpathScope>test</classpathScope>
                        </configuration>
                    </execution>
                    <execution>
                        <id>guest-recovery-verify</id>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                            <mainClass>com.automation.api.auth.VrgoGuestBrowserRecoveryVerifier</mainClass>
                            <classpathScope>test</classpathScope>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>test</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <env>test</env>
            </properties>
        </profile>
        <profile>
            <id>dev</id>
            <properties>
                <env>dev</env>
            </properties>
        </profile>
        <profile>
            <id>stage</id>
            <properties>
                <env>stage</env>
            </properties>
        </profile>
        <profile>
            <id>stage2</id>
            <properties>
                <env>stage2</env>
            </properties>
        </profile>
        <profile>
            <id>load</id>
            <properties>
                <env>load</env>
            </properties>
        </profile>
        <profile>
            <id>prod</id>
            <properties>
                <env>prod</env>
            </properties>
        </profile>
    </profiles>
</project>
